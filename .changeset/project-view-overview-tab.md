---
"@membank/dashboard": minor
---

Redesigned project view: synthesis and session context now live in a permanent Overview tab (default landing), with memories and activity as peer tabs. Opening a memory detail slides in as a right-side drawer overlay so the project context behind it is never unmounted.
