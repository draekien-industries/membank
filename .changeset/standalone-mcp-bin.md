---
"@membank/mcp": minor
---

Added standalone `membank-mcp` binary — harnesses can now be configured to run `npx @membank/mcp` directly, skipping CLI overhead. Invoking `membank --mcp` still works but now emits a deprecation warning.
