---
"@membank/cli": minor
---

Added config system and synthesis management commands. ConfigManager reads/writes ~/.membank/config.json. New commands: `membank config` (get/set/show) and `membank synthesize` (show/status). Synthesis opt-in prompt added to setup flow.
