---
"@membank/cli": patch
---

`setup --dry-run` now shows the config file path and CLI command for each MCP entry, the hook event and command for each injection hook, and the model name, cache path, and cache status instead of generic placeholder messages.
