---
"@membank/dashboard": minor
---

Added v2 dashboard with project-picker landing page and three-column workspace. The new UI is accessible at `/v2` and introduces per-project activity heatmaps, stats cards, and a workspace with a dedicated navigation sidebar, memory list, and detail panel — all scoped to a single project context.
